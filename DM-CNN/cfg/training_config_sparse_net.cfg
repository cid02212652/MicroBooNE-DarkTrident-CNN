input_train = "/workspace/cnn_datasets/MPID_training_set_full.root"
input_test  = "/workspace/cnn_datasets/MPID_test_set_full.root"
weights_directory = "/workspace/outputs/weights/sparsessnet_meanmax/"
output_directory  = "/workspace/outputs/training_metrics/"
seed = True
seed_number = 1
adc_lo=float(10)
adc_hi=float(500)
augment=False
batch_size_train=int(32)
batch_size_test=int(16)
drop_out=0.5
EPOCHS=5
GPUID=str("0")
learning_rate=float(0.001)
num_class=int(2)
plane=0
run_test=True
save_weights=True
test_events_nums=9
test_every_step=20
xdim=int(512)
ydim=int(512)

model = str("sparsessnet")   # options: "mpid", "resnet18", "resnet34", "sparsessnet"
resnet_pretrained = False    # True = ImageNet init (see note below)
in_channels = int(1)         # your data is 1-channel
gn_groups = int(32)


# --- SparseSSNet-style event classifier settings ---
# These are read by train_DM-CNN_sparseSSnet.py via getattr(..., default)
spatial_size = int(512)          # -ss in SparseSSNet repo
data_dim = int(2)                # 2D images
sparse_uresnet_num_strides = int(5)
sparse_uresnet_filters = int(16)
sparse_reps = int(2)
sparse_pool = str("meanmax")     # "mean", "max", or "meanmax"
sparse_dense_threshold = float(0.0)  # extra threshold before dens->sparse conversion
